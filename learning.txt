git config --global user.name "shiva srivastava"
git config --global user.email "hackingshiva13@gmail.com"
git --version

ssh-keygen -t ed25519 -C "mail@gmail.com"
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519

single time
git init
git status
git add <file-name>
git commit -m"message"
git restore <file-name>
git restore --staged <file-name></file-name>

echo "# git-learn-first" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/shiva-srivastav/git-learn-first.git
git push -u origin main

git push origin main
git add .